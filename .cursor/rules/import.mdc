---
description:
globs:
alwaysApply: true
---

- `import` 를 할 때에는 `tsconfig.json` 을 참조하여 `alias` 임포트를 항상 사용합니다.
- `@/---` 와 같은 형태가 되어야 합니다.
- `@` 는 `app` 폴더를 의미합니다.
- 상대 경로를 사용하지 않습니다.
- 백단의 기능과 프론트단의 기능을 명확하게 구분하여 임포트합니다.
